{
  "name": "http-server",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "dev": "node --watch src/index.ts",
    "start": "tsc -b && node dist/index.js"
  },
  "private": true,
  "devDependencies": {
    "@repo/typescript-config": "*",
    "@repo/eslint-config": "*",
    "@types/bun": "latest",
    "eslint": "^10.8.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/express": "^5.0.6",
    "express": "^5.2.1"
  }
}
